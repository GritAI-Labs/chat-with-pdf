# Pick ONE LLM backend. For a public deploy, use a cloud API (not local Ollama).
# Option A — Claude API (recommended):
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-haiku-4-5
# Option B — local Ollama (dev only; needs the fleet up):
# OLLAMA_URL=http://192.168.12.223:11434
# OLLAMA_MODEL=qwen2.5:7b
# (Embeddings run locally via fastembed — no key needed.)
